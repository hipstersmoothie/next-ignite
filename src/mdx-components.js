// TODO make pretty
const SidebarItem = props => <li style={{ color: "red" }} {...props} />;
// TODO make pretty
const SidebarLink = props => <a style={{ color: "red" }} {...props} />;

export default {
  SidebarItem,
  SidebarLink
}