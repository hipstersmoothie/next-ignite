---
layout: 'nav-bar'
---

# ignite

The most flexible documentation website you've ever used.
`ignite` is a thin wrapper around next-js.
If you wanted to you don't even need to use the `ignite` binary, you could use the function the package exports and put it into any `next.js` application!
